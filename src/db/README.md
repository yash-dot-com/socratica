### DB folder 
- contains the schema folder where all the database schema should be defined using drizzle
- don't confuse these schema with zod schemas. 
- all the schema files rests here.
- all the migration files (SQL generated) from "npx drizzle-kit generate" exists here.

